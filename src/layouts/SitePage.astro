---
import Layout from './Layout.astro';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';

interface Props {
  title: string;
  description?: string;
  active?: string;
  locale?: 'es' | 'en' | 'fr';
  availableLocales?: Array<'es' | 'en' | 'fr'>;
}

const { title, description = 'Movisafe', active = '', locale = 'es', availableLocales } = Astro.props;
---

<Layout title={title} description={description} lang={locale} availableLocales={availableLocales}>
  <SiteHeader active={active} locale={locale} />
  <slot />
  <SiteFooter locale={locale} />
</Layout>
